[tool.poetry]
name = "rag-api"
version = "0.1.0"
description = ""
authors = ["Your Name <you@example.com>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
fastapi = { extras = ["standard"], version = "^0.114.1" }
crawlee = { extras = ["beautifulsoup", "playwright"], version = "^0.3.5" }
html2text = "^2024.2.26"
chromadb = "^0.5.7"
langchain = "^0.3.0"
langchain-openai = "^0.2.0"
openai = "^1.51.1"
redis = "^5.1.1"
celery = "^5.4.0"
redisvl = "^0.3.5"


[tool.poetry.group.dev.dependencies]
celery-types = "^0.22.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
